import NotFoundCommon from "@components/common/not-found-common";

const NotFound = () => (
  <NotFoundCommon
    title="404 Page not found"
    subText="Sorry, but the page you were looking for could not be found"
  />
);

export default NotFound;
